__pycache__/
*.pyc
.venv/
uploads/
data/*.db
data/*.db-journal
seed/1_PROGRAMAS DE PRODUCCION MHC .xlsx
.env
zoho_config.json
supabase_config.json
# Datos del dashboard: viven en Supabase Storage (bucket privado 'bi'), no en el repo publico
docs/*.json
# Copia local de la ultima version buena de Z:\BI (fallback cuando el ERP esta exportando)
data/bi_cache/
# Llevan la API key de Anthropic en texto plano: nunca deben subirse al repo publico
crm.py
crm_server.py
netlify_functions/
